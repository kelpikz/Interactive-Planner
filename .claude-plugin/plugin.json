{
  "name": "plan-review",
  "version": "0.1.0",
  "description": "Review and iterate on Claude's plans using inline browser comments instead of linear chat.",
  "author": { "name": "ajithprasad004" },
  "mcpServers": {
    "plan-review": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.mjs"]
    }
  }
}
