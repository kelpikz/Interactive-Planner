{
  "name": "plan-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin: review and iterate on Claude's plans with inline browser comments instead of linear chat.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "marked": "^12.0.0",
    "zod": "^3.23.8"
  }
}
